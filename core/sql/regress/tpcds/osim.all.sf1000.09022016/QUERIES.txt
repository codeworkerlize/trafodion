--QueryShape: control query shape ;
set schema hive.tpcds_sf1000;;
--QueryShape: control query shape anything;
CONTROL QUERY DEFAULT SCHEMA'HIVE.TPCDS_SF1000';;
--QueryShape: control query shape anything;
select * from call_center;;
--QueryShape: control query shape scan(path 'HIVE.CALL_CENTER', forward, blocks_per_access 4 , mdam off);
select * from catalog_page;;
--QueryShape: control query shape scan(path 'HIVE.CATALOG_PAGE', forward, blocks_per_access 478 , mdam off);
select * from catalog_returns;;
--QueryShape: control query shape esp_exchange(scan(path 'HIVE.CATALOG_RETURNS', forward, blocks_per_access 520106 , mdam off));
select * from catalog_sales;;
--QueryShape: control query shape esp_exchange(scan(path 'HIVE.CATALOG_SALES', forward, blocks_per_access 4820798 , mdam off));
select * from customer;;
--QueryShape: control query shape esp_exchange(scan(path 'HIVE.CUSTOMER', forward, blocks_per_access 79774 , mdam off));
select * from customer_address;;
--QueryShape: control query shape esp_exchange(scan(path 'HIVE.CUSTOMER_ADDRESS', forward, blocks_per_access 3485 , mdam off));
select * from customer_demographics;;
--QueryShape: control query shape esp_exchange(scan(path 'HIVE.CUSTOMER_DEMOGRAPHICS', forward, blocks_per_access 2302 , mdam off));
select * from date_dim;;
--QueryShape: control query shape scan(path 'HIVE.DATE_DIM', forward, blocks_per_access 344 , mdam off);
select * from date_dim_54;;
--QueryShape: control query shape scan(path 'HIVE.DATE_DIM_54', forward, blocks_per_access 54 , mdam off);
select * from household_demographics;;
--QueryShape: control query shape scan(path 'HIVE.HOUSEHOLD_DEMOGRAPHICS', forward, blocks_per_access 2 , mdam off);
select * from income_band;;
--QueryShape: control query shape scan(path 'HIVE.INCOME_BAND', forward, blocks_per_access 2 , mdam off);
select * from inventory;;
--QueryShape: control query shape esp_exchange(scan(path 'HIVE.INVENTORY', forward, blocks_per_access 247681 , mdam off));
select * from item;;
--QueryShape: control query shape esp_exchange(scan(path 'HIVE.ITEM', forward, blocks_per_access 1495 , mdam off));
select * from promotion;;
--QueryShape: control query shape scan(path 'HIVE.PROMOTION', forward, blocks_per_access 30 , mdam off);
select * from reason;;
--QueryShape: control query shape scan(path 'HIVE.REASON', forward, blocks_per_access 1 , mdam off);
select * from reason_1;;
--QueryShape: control query shape scan(path 'HIVE.REASON_1', forward, blocks_per_access 1 , mdam off);
select * from reason_2;;
--QueryShape: control query shape scan(path 'HIVE.REASON_2', forward, blocks_per_access 1 , mdam off);
select * from ship_mode;;
--QueryShape: control query shape scan(path 'HIVE.SHIP_MODE', forward, blocks_per_access 1 , mdam off);
select * from store;;
--QueryShape: control query shape scan(path 'HIVE.STORE', forward, blocks_per_access 31 , mdam off);
select * from store_returns;;
--QueryShape: control query shape esp_exchange(scan(path 'HIVE.STORE_RETURNS', forward, blocks_per_access 791126 , mdam off));
select * from store_sales;;
--QueryShape: control query shape esp_exchange(scan(path 'HIVE.STORE_SALES', forward, blocks_per_access 6278932 , mdam off));
select * from store_sales_1stripeperfile;;
--QueryShape: control query shape esp_exchange(scan(path 'HIVE.STORE_SALES_1STRIPEPERFILE', forward, blocks_per_access 6277667 , mdam off));
select * from store_sales_sorted_orc_item;;
--QueryShape: control query shape scan(path 'HIVE.STORE_SALES_SORTED_ORC_ITEM', forward, blocks_per_access 0 , mdam off);
select * from store_sales_sorted_orc_store;;
--QueryShape: control query shape scan(path 'HIVE.STORE_SALES_SORTED_ORC_STORE', forward, blocks_per_access 0 , mdam off);
select * from store_sales_sorted_orc_time;;
--QueryShape: control query shape scan(path 'HIVE.STORE_SALES_SORTED_ORC_TIME', forward, blocks_per_access 0 , mdam off);
select * from time_dim;;
--QueryShape: control query shape scan(path 'HIVE.TIME_DIM', forward, blocks_per_access 148 , mdam off);
select * from warehouse;;
--QueryShape: control query shape scan(path 'HIVE.WAREHOUSE', forward, blocks_per_access 1 , mdam off);
select * from web_page;;
--QueryShape: control query shape scan(path 'HIVE.WEB_PAGE', forward, blocks_per_access 46 , mdam off);
select * from web_returns;;
--QueryShape: control query shape esp_exchange(scan(path 'HIVE.WEB_RETURNS', forward, blocks_per_access 241928 , mdam off));
select * from web_sales;;
--QueryShape: control query shape esp_exchange(scan(path 'HIVE.WEB_SALES', forward, blocks_per_access 2572862 , mdam off));
select * from web_site;;
--QueryShape: control query shape scan(path 'HIVE.WEB_SITE', forward, blocks_per_access 4 , mdam off);
control osim capture stop;;
